ตัวแปรสำคัญ

- $_SESSION["EWT_SDB"] = ตัวแปร database

- $_SESSION["EWT_SUSER"] = ตัวแปรชื่อ folder ของ front end

connect database ไว้แล้ว

เวลาใช้ใช้ class db

query ใช้ $db->query("SELECT * FROM ......");

$db->db_num_rows(); = mysql_num_rows();

$db->db_fetch_row(); = mysql_fetch_row();

$db->db_fetch_array(); = mysql_fetch_array();


pdftoimage -r YKEJEHZHHBCDQMYM